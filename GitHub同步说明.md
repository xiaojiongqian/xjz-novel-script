# GitHub 自动同步

本项目已经配置为自动同步到 GitHub 的 `main` 分支。

## 日常使用

自动同步启用后，无须运行 Git 命令。正常编辑并保存文件即可：

1. 后台程序每 10 秒检查一次目录变化。
2. 有变化时自动创建本地提交。
3. 自动吸收 GitHub 上可能存在的新提交。
4. 将本地更新推送到 GitHub。

网络临时中断时，本地文件和本地提交都会保留。后台程序会在下一轮继续尝试，不会强制覆盖文件。

## 手动操作入口

- `GitHub立即同步.cmd`：立即执行一次同步。
- `开启GitHub自动同步.cmd`：启用自动同步，并设置为登录 Windows 后自动启动。
- `关闭GitHub自动同步.cmd`：关闭后台同步和开机启动。

## 冲突保护

以下情况会暂停同步并保留现场：

- 当前不在 `main` 分支。
- Git 正在执行合并或变基。
- 远程更新产生需要人工决定的冲突。
- GitHub 登录失效或网络不可用。

运行记录保存在：

`%LOCALAPPDATA%\xjz-novel-script\github-sync.log`
